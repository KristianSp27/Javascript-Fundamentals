function reversedChars(charA, charB, charC) {
  let space = "";
  let finalCharA = charA + space;
  let finalCharB = charB + space;
  let finalCharC = charC + space;
  console.log(`${finalCharC} ${finalCharB} ${finalCharA} `);
}
